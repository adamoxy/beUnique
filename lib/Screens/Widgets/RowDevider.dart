import 'package:flutter/material.dart';

class RowDevider extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: new BoxDecoration(
        color: Color(0xFF0A2175),
      ),
      height: 1,
    );
  }
}
